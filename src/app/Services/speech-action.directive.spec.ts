import { SpeechActionDirective } from './speech-action.directive';

describe('SpeechActionDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeechActionDirective();
    expect(directive).toBeTruthy();
  });
});
