import { SpeechContextDirective } from './speech-context.directive';

describe('SpeechContextDirective', () => {
  it('should create an instance', () => {
    const directive = new SpeechContextDirective();
    expect(directive).toBeTruthy();
  });
});
